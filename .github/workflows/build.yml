name: Build Android APK
on:
  push:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Build Tools
        run: |
          pip install flet yt-dlp
          
      - name: Build APK
        run: |
          # 1. Create the assets folder (Flet 0.80+ requirement)
          mkdir -p assets
          
          # 2. Move your icon into the folder so Flet finds it automatically
          if [ -f "icon.png" ]; then
            cp icon.png assets/icon.png
          fi
          
          # 3. Build command (Removed --icon and --splash flags)
          flet build apk \
            --product "bobsiclesMp3s" \
            --android-permissions "android.permission.WRITE_EXTERNAL_STORAGE=True android.permission.READ_EXTERNAL_STORAGE=True" \
            --verbose
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: bobsiclesMp3s-APK
          path: build/apk/*.apk
